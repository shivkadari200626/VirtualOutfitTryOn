workflows:
  android-release:
    name: Build APK
    scripts:
      - name: Build
        script: |
          chmod +x ./gradlew
          ./gradlew clean assembleRelease
    artifacts:
      - app/build/outputs/apk/release/app-release.apk
